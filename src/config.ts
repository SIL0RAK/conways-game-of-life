export enum Speed {
    Min = 10,
    Max = 1500,
    Default = 1000,
}

export enum MatrixSize {
    DefaultWidth = 10,
    DefaultHeight = 10,
    MaxWidth = 100,
    MaxHeight = 100,
    MinWidth = 1,
    MinHeight = 1,
}